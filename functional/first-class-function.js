/*
 *  A programming language is said to have 
 *  First-Class functions when functions in 
 *  that language are trated like any other 
 *  variable.
*/

// Funções são tratadas como var

// example
const add = function (a,b) {
    return a + b
}

console.log(add(1,2))


/*
    Functions that operate other functions, 
    either by taking them as arguments or by
    returning them, are called higher-order functions 
*/
/*
    High-order class function é quando vc retorna uma função dentro da função, ou passa uma função como argumento
*/